<% include ./partials/head.ejs %>
<% include ./partials/nav.ejs %>


<h2> This is Favorite List </h2>

<% data.forEach(data => { %>

<h2><%= data.dba %></h2>
<form id="delete-form" action=<%= `profile/${data.id}?_method=DELETE` %> method="POST"> 
	<input type="submit" value='<i class="fa fa-minus-circle" aria-hidden="true"></i>'/>
	
</form>
<div class="address"><%= `${data.building} ${data.street}, ${data.boro}, ${data.zipcode}`%> 

<% }) %> 

</body>
</html>

id: 16,
    